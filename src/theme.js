import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0000FF', // Change this to your primary color
    },
    secondary: {
      main: '#0000FF', // Change this to your secondary color
    },
  },
});

export default theme;
